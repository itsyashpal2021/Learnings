**We have `ClassCounter` class which has `count` instance variable and `increment` method to increment count.**
- Write a code to demonstrate declaring count as volatile will not work with multiple threads invoking increment.
- What changes are needed for this to work with multiple threads.
- Do we need both `synchronized` and `volatile` ?